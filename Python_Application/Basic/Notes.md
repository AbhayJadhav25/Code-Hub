# Range  : 
It's data type , not a function. belongs to the sequence catogery.
range(5)  --> 0,1 , 2, 3, 4
range(4)  --> 0,1,2,3
range(3,7)  --> 3,4,5,6
range(8,3)  -> null 
range(8,3,-1)  --> reverse 8 to 4
# Function : 
$also called as API , Method , Procedure

#Python allocate the memory when we required

lambda is mostly used in Filter , Map , reduce

# Every process(pid) and thread has unique number.
#for Calculation there is always cpu bound process

#to remove the confusion between instance method and class method there is a concept called decorator